<% include ../partials/header.ejs %>

<div class="container">
    <div class="row">
        <!-- MOVE STYLE TAGS TO CSS FILE AND DELETE THIS COMMENT -->
        <h1 class="formHeader">Edit Review</h1>
        <div class="formDiv">
            
            <!-- ADD SANITIZER AND DELETE THIS COMMENT -->
            <!-- EDIT REVIEW FORM -->
            <form action="/books/<%= book_id %>/reviews/<%= review._id %>?_method=PUT" method="POST">
                <div class="form-group">
                    <input class="form-control" type="text" name="review[text]" value="<%= review.text %>">
                </div>
                <div class="form-group">
                    <button class="btn btn-lg btn-primary btn-block">Submit Changes</button>
                </div>
            </form>
            
            <!-- LINK BACK TO INDEX PAGE -->
            <a href="/books">Go Back</a>
        </div>
    </div>
</div>

<% include ../partials/footer.ejs %>